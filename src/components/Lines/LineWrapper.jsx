const LineWrapper = ({ key, lineWidth }) => {
    <div
        key={el}
        style={{ width: `${lineWidth}px` }}
        className={`
            h-full bg-[#73bf2e]
        `}>
            
    </div>
}

export default LineWrapper;