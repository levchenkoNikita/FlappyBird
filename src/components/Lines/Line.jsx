import React from 'react';

const Line = React.memo(() => {
    return (
        <div
            className={`
                w-[50%] h-full bg-[#73bf2e] -skew-x-42
            `}>

        </div>
    );
})

export default Line;